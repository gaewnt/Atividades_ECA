#include <stdio.h>
#include <math.h>

int main(void)
{
    int a, b, c;
    scanf("%d",&a);
    scanf("%d",&b);
    c = a % b;
    if (c == 0){
        printf("Eh divisivel");
    }else
    if (c != 0){
        printf("Nao eh divisivel");
    }
}